import { Container, Typography, Box } from "@material-ui/core";

const Calendar = () => {
  return (
    <Container>
      <Box mb={4}>
        <Typography variant='h5' style={{ color: "#fff" }}>
          Calendar (Yapım Aşamasında)
        </Typography>
      </Box>
    </Container>
  );
};

export default Calendar;
