import { Container, Typography, Box } from "@material-ui/core";

const Trends = () => {
  return (
    <Container>
      <Box mb={4}>
        <Typography variant='h5' style={{ color: "#fff" }}>
          Trends (Yapım Aşamasında)
        </Typography>
      </Box>
    </Container>
  );
};

export default Trends;
