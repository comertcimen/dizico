import { Container, Typography, Box } from "@material-ui/core";

const Watchtogether = () => {
  return (
    <Container>
      <Box mb={4}>
        <Typography variant='h5' style={{ color: "#fff" }}>
          Watch Together (Yapım Aşamasında)
        </Typography>
      </Box>
    </Container>
  );
};

export default Watchtogether;
