import { Container, Typography, Box } from "@material-ui/core";

const Discover = () => {
  return (
    <Container>
      <Box mb={4}>
        <Typography variant='h5' style={{ color: "#fff" }}>
          Discover (Yapım Aşamasında)
        </Typography>
      </Box>
    </Container>
  );
};

export default Discover;
