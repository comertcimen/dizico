import { Container, Typography, Box } from "@material-ui/core";

const Collections = () => {
  return (
    <Container>
      <Box mb={4}>
        <Typography variant='h5' style={{ color: "#fff" }}>
          Collections (Yapım Aşamasında)
        </Typography>
      </Box>
    </Container>
  );
};

export default Collections;
